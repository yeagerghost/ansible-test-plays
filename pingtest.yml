---

- hosts: all
  gather_facts: false
  remote_user: ubuntu
  vars:
    user: 'francis'
  tasks:
    - name: Ping task
      ping:

    - name: Debug task 1
      debug: 
        msg="{{ lookup('vars',user)['ssh_pubkey'] }}"